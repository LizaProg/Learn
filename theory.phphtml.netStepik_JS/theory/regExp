. - любой символ
+ (один и более раз)
* (ноль и более раз)
? (ноль или один раз, иначе говоря - может быть, а может не быть)
+?, -?, *?, ??, {}?  и тд это ограничение жадности
{} количество повторений ({5} – пять повторений,{2, 5}повторяется от двух до пяти (оба включительно), {2,} - повторяется два и более раз. варианта - {,2} - нет.)
^ отрицание:(/[^abc]xx/g)первый символ - это НЕ буква 'a', 'b' или 'c' (любой символ кроме них), потом две буквы 'x'.
^ начало строки
$ конец строки
| или


\s	Обозначает пробел или пробельный символ (имеется ввиду перевод строки, табуляция и т.п.).
\S	Не пробел, то есть всё, кроме \s.
\w	Цифра или буква (кириллица не входит в буквы).
\W	Не цифра и не буква.
\d	Цифра от 0 до 9.
\D	Не цифра от 0 до 9.

[a-z] задаст маленькие латинские буквы,
[A-Z] – большие, [0-9] – цифру от 0 до 9
[a-zA-Z] – большие и маленькие латинские буквы,
[a-zA-Z0-9] большие и маленькие латинские буквы, плюс цифры
[2-5] - цифра от 2-х до 5-ти (обе включительно),
[a-c] – буквы от 'a' до 'c' по алфавиту (то есть 'a', 'b', 'c') и так далее.
[а-яА-ЯЁё] - кириллица + Ёё


Являются спецсимволами: $ ^ . * + ? \ {} [] () |
Не являются спецсимволами: @ : , ' '' ; - _ = < > % # ~ `& ! /