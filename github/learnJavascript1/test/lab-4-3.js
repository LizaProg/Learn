//записать в массив b квадраты значений массива а с помощью цикла for,
// с помощью цикла for/in


var a = [5, 12];
var b = [];
a [99] = 7;

for (var i in a) {
    b.push(Math.pow(a[i], 2));
}
console.log(b);

for (var i = 0; i ; )
