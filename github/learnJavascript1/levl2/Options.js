Option(text/*то что между тэгами*/, value/*значение*/, defaultSelected/*булев тип*/, selected/*сделать его сейчас*/)

var o = new Option('PHP5', 'php');

selected.add(newOpt, curOpt);
selected.add(newOpt, curOptIdx); //IE

selected.remove(index);